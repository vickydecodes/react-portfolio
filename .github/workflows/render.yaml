name: Deploy to Render

on:
  push:
    branches:
      - master  # or your deploy branch

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render Deployment
        run: |
          curl "https://api.render.com/deploy/srv-cra37ci3esus739q5qcg?key=R-WsgK0Y3Ig"
        env:
          RENDER_DEPLOY_HOOK: "https://api.render.com/deploy/srv-cra37ci3esus739q5qcg?key=R-WsgK0Y3Ig"
